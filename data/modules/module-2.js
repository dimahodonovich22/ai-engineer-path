// Модуль 2 «Python: логика» — списки, циклы, словари, функции.
export default {
  id: 'm2',
  lessons: [
    {
      id: 'm2-l1',
      title: 'Списки (lists)',
      minutes: 9,
      xp: 30,
      cards: [
        {
          title: 'Одна коробка для многих значений',
          html: '<p>Представь чат с ИИ: сообщений может быть и три, и триста. Заводить переменную под каждое — безумие. Для этого есть <strong>список</strong> (list) — коробка, которая хранит сколько угодно значений по порядку.</p>',
        },
        {
          title: 'Создаём список',
          html: '<p>Список пишется в <strong>квадратных скобках</strong>, элементы разделяются запятыми. Вот история сообщений чата:</p>',
          code: 'messages = ["Привет!", "Как дела?", "Расскажи про Python"]\nprint(messages)',
        },
        {
          title: 'Индексы: отсчёт с нуля',
          html: '<p>У каждого элемента есть номер — <strong>индекс</strong>. Важно: Python считает <strong>с нуля</strong>! Первый элемент — <code>[0]</code>, второй — <code>[1]</code>, третий — <code>[2]</code>.</p>',
          code: 'print(messages[0])  # Привет!\nprint(messages[2])  # Расскажи про Python',
        },
        {
          title: 'Отрицательные индексы',
          html: '<p>А если нужен <em>последний</em> элемент, а длину списка ты не знаешь? Используй отрицательный индекс: <code>[-1]</code> — последний, <code>[-2]</code> — предпоследний. Так удобно брать самое свежее сообщение чата.</p>',
          code: 'print(messages[-1])  # Расскажи про Python\nprint(messages[-2])  # Как дела?',
        },
        {
          title: 'Список можно менять',
          html: '<p>В отличие от строк, список — изменяемый: можно заменить любой элемент по индексу. И хранить в нём можно что угодно: строки, числа и даже другие списки.</p>',
          code: 'messages[0] = "Здравствуй!"\nprint(messages)  # ["Здравствуй!", "Как дела?", "Расскажи про Python"]',
        },
      ],
      drills: [
        {
          afterCard: 1,
          q: "Замени ___ на список из трёх строк \"Привет!\", \"Как дела?\", \"Расскажи про Python\" — в квадратных скобках, через запятую. Ожидаемый вывод: ['Привет!', 'Как дела?', 'Расскажи про Python']",
          starter: '# Замени ___ на список из трёх строк\nmessages = ___\nprint(messages)',
          expect: "['Привет!', 'Как дела?', 'Расскажи про Python']",
          solution: 'messages = ["Привет!", "Как дела?", "Расскажи про Python"]\nprint(messages)',
          hints: [
            'Список пишется в квадратных скобках, элементы-строки — в кавычках и через запятую.',
            'messages = [___, "Как дела?", "Расскажи про Python"] — добавь первую строку.',
            'messages = ["Привет!", "Как дела?", ___] — остался последний элемент "Расскажи про Python".',
          ],
          explain: 'Квадратные скобки создают список. Заметь: print показывает строки списка в одинарных кавычках — так Python печатает списки.',
        },
        {
          afterCard: 2,
          q: 'В заготовке — список prompts. Выведи по индексам два элемента: сначала первый («сократи»), затем третий («объясни») — каждый на своей строке.',
          starter: '# Список промптов\nprompts = ["сократи", "переведи", "объясни"]\n',
          expect: 'сократи\nобъясни',
          solution: 'prompts = ["сократи", "переведи", "объясни"]\nprint(prompts[0])\nprint(prompts[2])',
          hints: [
            'Счёт идёт с нуля: у первого элемента индекс 0, у третьего — 2.',
            'print(prompts[___])\nprint(prompts[___]) — подставь два индекса.',
            'print(prompts[0])\nprint(prompts[___]) — во втором print индекс третьего элемента: на единицу меньше трёх.',
          ],
          explain: 'Индексы идут с нуля: [0] — первый, [2] — третий. Это главная ловушка списков — и ты её только что обошёл.',
        },
        {
          afterCard: 3,
          q: 'Выведи последнее сообщение history отрицательным индексом — без подсчёта длины списка (получится «пока»).',
          starter: '# История чата\nhistory = ["привет", "как дела?", "пока"]\n',
          expect: 'пока',
          solution: 'history = ["привет", "как дела?", "пока"]\nprint(history[-1])',
          hints: [
            'Отрицательный индекс считает с конца: -1 — последний элемент.',
            'print(history[___]) — подставь отрицательный индекс последнего элемента.',
            'print(history[-___]) — после минуса стоит единица.',
          ],
          explain: 'Индекс -1 достаёт последний элемент, какой бы длины ни был список — не нужно знать его длину.',
        },
        {
          afterCard: 4,
          q: "С чистого листа: создай список models = [\"gpt\", \"claude\"], замени первый элемент на \"gemini\" и выведи список. Ожидаемый вывод: ['gemini', 'claude']",
          starter: '# Создай список, замени первый элемент, выведи результат\n',
          expect: "['gemini', 'claude']",
          solution: 'models = ["gpt", "claude"]\nmodels[0] = "gemini"\nprint(models)',
          hints: [
            'Замена — это обычное присваивание по индексу, а первый элемент имеет индекс 0.',
            'models = ["gpt", "claude"]\nmodels[___] = "gemini"\nprint(models)',
            'models = ["gpt", "claude"]\nmodels[0] = ___\nprint(models) — на месте пропуска строка "gemini".',
          ],
          explain: 'Присваивание по индексу меняет список на месте — списки изменяемые, в отличие от строк.',
        },
      ],
      quiz: [
        {
          type: 'mcq',
          q: 'С какого числа начинается нумерация элементов списка в Python?',
          options: ['С 1', 'С 0', 'С -1', 'Можно выбрать самому'],
          correct: 1,
          explain: 'Индексация всегда начинается с нуля: первый элемент — [0]. Это одна из самых частых ошибок новичков, запомни её сейчас.',
        },
        {
          type: 'fill',
          q: 'Достань последнее сообщение из истории чата:',
          code: 'history = ["привет", "как дела?", "пока"]\nprint(history[___])',
          options: ['3', '-1', '1'],
          correct: 1,
          explain: 'Индекс -1 всегда указывает на последний элемент, какой бы длины ни был список. Индекса 3 здесь нет — элементы занимают индексы 0, 1 и 2.',
        },
        {
          type: 'tf',
          q: 'В списке из трёх элементов последний имеет индекс 3.',
          correct: false,
          explain: 'Из-за отсчёта с нуля три элемента занимают индексы 0, 1 и 2. Последний индекс всегда на единицу меньше длины списка.',
        },
        {
          type: 'mcq',
          q: 'prompts = ["один", "два", "три"] — что выведет print(prompts[1])?',
          options: ['один', 'три', 'два', 'Ошибка'],
          correct: 2,
          explain: 'Индекс 1 — это второй элемент, потому что счёт идёт с нуля: prompts[0] — «один», prompts[1] — «два».',
        },
        {
          type: 'code',
          q: 'История чата лежит в списке messages. Выведи первое сообщение («Привет!»), а на следующей строке — последнее («Расскажи про Python»), используя индексы.',
          starter: '# История чата уже готова — достань элементы по индексам\nmessages = ["Привет!", "Как дела?", "Расскажи про Python"]\n',
          expect: 'Привет!\nРасскажи про Python',
          solution: 'messages = ["Привет!", "Как дела?", "Расскажи про Python"]\nprint(messages[0])\nprint(messages[-1])',
          hints: [
            'Первый элемент — индекс 0 (счёт с нуля!), а последний удобно взять отрицательным индексом.',
            'print(messages[___])\nprint(messages[___]) — подставь индексы первого и последнего элементов.',
            'print(messages[0])\nprint(messages[___]) — последний элемент по индексу -1.',
          ],
          explain: 'Индексация начинается с нуля, поэтому первое сообщение — [0]. А индекс -1 всегда указывает на последний элемент, какой бы длины ни был список — не нужно знать его длину.',
        },
      ],
      practice: null,
    },
    {
      id: 'm2-l2',
      title: 'Списки: методы и срезы',
      minutes: 10,
      xp: 30,
      cards: [
        {
          title: 'append() — добавить в конец',
          html: '<p>История диалога с ИИ растёт с каждой репликой. Метод <code>append()</code> добавляет новый элемент в конец списка — именно так чат-боты копят сообщения.</p>',
          code: 'history = ["Привет!"]\nhistory.append("Чем занимаешься?")\nprint(history)  # ["Привет!", "Чем занимаешься?"]',
        },
        {
          title: 'len() — сколько элементов',
          html: '<p>Функция <code>len()</code> возвращает длину списка. Пригодится, чтобы узнать, сколько сообщений накопилось в диалоге.</p>',
          code: 'print(len(history))  # 2',
        },
        {
          title: 'in — есть ли элемент',
          html: '<p>Оператор <code>in</code> проверяет, есть ли значение в списке, и возвращает <code>True</code> или <code>False</code>. Отлично работает в паре с <code>if</code>.</p>',
          code: 'if "Привет!" in history:\n    print("Пользователь уже здоровался")',
        },
        {
          title: 'Срезы (slices)',
          html: '<p><strong>Срез</strong> вырезает кусок списка: <code>history[1:3]</code> — элементы с индекса 1 <em>до</em> 3, не включая сам 3. Правая граница не входит — так уж договорились в Python.</p>',
          code: 'msgs = ["a", "b", "c", "d"]\nprint(msgs[1:3])  # ["b", "c"]',
        },
        {
          title: 'Последние N сообщений',
          html: '<p>У моделей ИИ ограничен объём контекста, поэтому в API часто отправляют не всю историю, а <strong>последние N сообщений</strong>. Срез с отрицательным началом делает это в одну строку.</p>',
          code: 'last_two = msgs[-2:]\nprint(last_two)  # ["c", "d"]',
        },
      ],
      drills: [
        {
          afterCard: 0,
          q: "Замени ___ на метод, который добавляет элемент в конец списка. Ожидаемый вывод: ['Привет!', 'Чем занимаешься?']",
          starter: '# Замени ___ на нужный метод\nhistory = ["Привет!"]\nhistory.___("Чем занимаешься?")\nprint(history)',
          expect: "['Привет!', 'Чем занимаешься?']",
          solution: 'history = ["Привет!"]\nhistory.append("Чем занимаешься?")\nprint(history)',
          hints: [
            'Метод добавления в конец вызывается через точку — его имя есть в заголовке карточки.',
            'history.___("Чем занимаешься?") — шесть букв, начинается на «a».',
            'history.app___("Чем занимаешься?") — допиши окончание end.',
          ],
          explain: 'append() добавляет элемент в конец списка — именно так чат-боты копят историю сообщений.',
        },
        {
          afterCard: 1,
          q: 'Выведи количество сообщений в списке history одним print (получится 3).',
          starter: '# История диалога\nhistory = ["привет", "как дела?", "пока"]\n',
          expect: '3',
          solution: 'history = ["привет", "как дела?", "пока"]\nprint(len(history))',
          hints: [
            'Длину списка возвращает функция len() — её результат можно сразу передать в print.',
            'print(___(history)) — впиши имя функции.',
            'print(len(___)) — в скобки передай список history.',
          ],
          explain: 'len() считает элементы списка. Чат-боты так узнают, сколько сообщений накопилось в диалоге.',
        },
        {
          afterCard: 2,
          q: 'Через if и оператор in проверь, есть ли в history строка "привет", и если есть — выведи "Пользователь уже здоровался".',
          starter: '# История диалога\nhistory = ["привет", "как дела?", "пока"]\n',
          expect: 'Пользователь уже здоровался',
          solution: 'history = ["привет", "как дела?", "пока"]\nif "привет" in history:\n    print("Пользователь уже здоровался")',
          hints: [
            'Оператор in возвращает True или False — такое выражение можно поставить прямо после if.',
            'if "привет" ___ history:\n    print("Пользователь уже здоровался")',
            'if ___ in history:\n    print("Пользователь уже здоровался") — на месте пропуска строка "привет".',
          ],
          explain: 'in вернул True, условие сработало — print с отступом выполнился. Так боты проверяют, было ли что-то в диалоге.',
        },
        {
          afterCard: 4,
          q: "С чистого листа: создай список msgs = [\"a\", \"b\", \"c\", \"d\", \"e\"] и выведи последние два элемента одним срезом. Ожидаемый вывод: ['d', 'e']",
          starter: '# Создай список и возьми последние два элемента срезом\n',
          expect: "['d', 'e']",
          solution: 'msgs = ["a", "b", "c", "d", "e"]\nprint(msgs[-2:])',
          hints: [
            'Срез «от второго с конца и до конца» пишется с отрицательным началом и пустым концом.',
            'msgs = ["a", "b", "c", "d", "e"]\nprint(msgs[___:]) — подставь отрицательное начало среза.',
            'print(msgs[-2___]) — не забудь двоеточие: без него получится один элемент, а не срез.',
          ],
          explain: 'Срез [-2:] значит «от второго с конца до конца» — так в API отправляют последние N сообщений, когда контекст ограничен.',
        },
      ],
      quiz: [
        {
          type: 'mcq',
          q: 'Какой метод добавляет элемент в конец списка?',
          options: ['add()', 'append()', 'push()', 'insert()'],
          correct: 1,
          explain: 'В Python это append(). Методы push() и add() — из других языков, они тут не сработают.',
        },
        {
          type: 'fill',
          q: 'Возьми последние два сообщения для отправки в API:',
          code: 'history = ["a", "b", "c", "d"]\nlast = history___',
          options: ['[-2:]', '[2]', '[:2]', '[-2]'],
          correct: 0,
          explain: 'Срез [-2:] означает «от второго с конца и до конца» — получим ["c", "d"]. А [:2] взял бы первые два, [2] и [-2] — по одному элементу.',
        },
        {
          type: 'tf',
          q: 'Выражение "привет" in ["привет", "пока"] вернёт True.',
          correct: true,
          explain: 'Оператор in проверяет наличие значения в списке. Строка «привет» там есть, значит результат — True.',
        },
        {
          type: 'mcq',
          q: 'Что вернёт len(["сократи", "переведи", "объясни"])?',
          options: ['2', '3', '4', 'Ошибка'],
          correct: 1,
          explain: 'len() считает количество элементов, а не индексы. Элементов три — ответ 3, хотя последний индекс равен 2.',
        },
        {
          type: 'order',
          q: 'Расставь шаги накопления истории чата по порядку:',
          items: [
            'Создать пустой список history = []',
            'Добавить новое сообщение через append()',
            'Узнать число сообщений через len()',
            'Взять последние сообщения срезом для API',
          ],
          explain: 'Сначала заводим пустую историю, наполняем её append(), а когда сообщений становится много — отправляем в API только свежий кусок среза.',
        },
        {
          type: 'code',
          q: 'Добавь в history сообщение "пока" через append(). Затем выведи длину списка (получится 4), а на следующей строке — последние два сообщения срезом.',
          starter: '# История диалога с ИИ\nhistory = ["привет", "как дела?", "объясни списки"]\n# Добавь "пока", выведи длину и последние два сообщения\n',
          expect: "4\n['объясни списки', 'пока']",
          solution: 'history = ["привет", "как дела?", "объясни списки"]\nhistory.append("пока")\nprint(len(history))\nprint(history[-2:])',
          hints: [
            'Три шага: добавить "пока" методом append(), напечатать длину через len(), напечатать срез «последние два».',
            'history.append(___)\nprint(len(history))\nprint(history[-2:])',
            'history.append("пока")\nprint(len(history))\nprint(history[___]) — срез последних двух: -2 и двоеточие.',
          ],
          explain: 'append() добавил элемент в конец, len() посчитал все четыре, а срез [-2:] взял два последних. Обрати внимание: print списка показывает строки в кавычках — так Python печатает списки.',
        },
      ],
      practice: null,
    },
    {
      id: 'm2-l3',
      title: 'Цикл for',
      minutes: 9,
      xp: 30,
      cards: [
        {
          title: 'Зачем нужны циклы (loops)',
          html: '<p>У тебя список из 50 промптов, и каждый нужно напечатать. Писать 50 строк <code>print()</code>? Нет — для повторяющихся действий есть <strong>цикл</strong> (loop). Он выполняет один и тот же код для каждого элемента.</p>',
        },
        {
          title: 'for перебирает список',
          html: '<p>Цикл <code>for</code> берёт элементы списка по одному и кладёт в переменную. Читается почти как английский: «для каждого prompt в prompts — сделай…».</p>',
          code: 'prompts = ["Сократи текст", "Переведи на английский", "Объясни просто"]\nfor prompt in prompts:\n    print(prompt)',
        },
        {
          title: 'Отступ — это тело цикла',
          html: '<p>Всё, что написано <strong>с отступом</strong> после двоеточия, повторяется на каждом шаге. Строки без отступа выполнятся один раз — уже после цикла. Ты помнишь это правило по <code>if</code> — здесь оно то же самое.</p>',
          code: 'for prompt in prompts:\n    print("Промпт:", prompt)\nprint("Готово!")  # выполнится один раз',
        },
        {
          title: 'range() — цикл по числам',
          html: '<p>Нужно повторить действие N раз без списка? Функция <code>range(3)</code> даёт числа <strong>0, 1, 2</strong> — три числа, начиная с нуля. Верхняя граница, как и в срезах, не включается.</p>',
          code: 'for i in range(3):\n    print("Попытка номер", i)\n# Попытка номер 0\n# Попытка номер 1\n# Попытка номер 2',
        },
        {
          title: 'for + f-строка',
          html: '<p>Внутри цикла работает всё, что ты уже знаешь: f-строки, условия, арифметика. Так можно, например, красиво пронумеровать промпты для отчёта.</p>',
          code: 'for prompt in prompts:\n    print(f"Обрабатываю: {prompt}")',
        },
      ],
      drills: [
        {
          afterCard: 1,
          q: 'Замени ___ на ключевое слово из заголовка цикла — каждый промпт напечатается на своей строке.',
          starter: '# Замени ___ на ключевое слово\nprompts = ["Сократи текст", "Переведи на английский", "Объясни просто"]\nfor prompt ___ prompts:\n    print(prompt)',
          expect: 'Сократи текст\nПереведи на английский\nОбъясни просто',
          solution: 'prompts = ["Сократи текст", "Переведи на английский", "Объясни просто"]\nfor prompt in prompts:\n    print(prompt)',
          hints: [
            'Заголовок цикла читается как английская фраза: «для каждого prompt В prompts».',
            'for prompt ___ prompts: — то же слово, каким ты проверял «есть ли элемент в списке».',
            'for prompt i___ prompts: — двухбуквенное слово, допиши вторую букву.',
          ],
          explain: 'Цикл for берёт элементы по одному и кладёт в переменную prompt. Три элемента — три витка, три строки.',
        },
        {
          afterCard: 2,
          q: 'Циклом for выведи каждый промпт из списка, а ПОСЛЕ цикла — одну строку «Готово!». Всего три строки вывода.',
          starter: '# Промпты\nprompts = ["сократи", "переведи"]\n',
          expect: 'сократи\nпереведи\nГотово!',
          solution: 'prompts = ["сократи", "переведи"]\nfor p in prompts:\n    print(p)\nprint("Готово!")',
          hints: [
            'Строка с отступом повторяется на каждом витке, строка без отступа выполнится один раз — после цикла.',
            'for p in prompts:\n    print(___)\nprint("Готово!")',
            'for p in prompts:\n    print(p)\n___("Готово!") — финальная строка пишется БЕЗ отступа.',
          ],
          explain: 'print(p) с отступом сработал на каждом витке, а print("Готово!") без отступа — один раз после цикла. Отступ и есть граница тела.',
        },
        {
          afterCard: 3,
          q: 'Через range() выведи три строки: «Попытка номер 0», «Попытка номер 1», «Попытка номер 2».',
          starter: '# Цикл по числам через range\n',
          expect: 'Попытка номер 0\nПопытка номер 1\nПопытка номер 2',
          solution: 'for i in range(3):\n    print("Попытка номер", i)',
          hints: [
            'range(3) даёт числа 0, 1, 2 — тройка не включается, как правая граница среза.',
            'for i in range(___):\n    print("Попытка номер", i)',
            'for i in range(3):\n    print("Попытка номер", ___) — в print через запятую передай счётчик i.',
          ],
          explain: 'range(3) начинает с нуля и не включает тройку — получаются 0, 1, 2. Та же логика, что у индексов и срезов.',
        },
        {
          afterCard: 4,
          q: 'С чистого листа: создай список prompts = ["сократи", "переведи"] и циклом с f-строкой выведи для каждого промпта строку «Обрабатываю: <промпт>».',
          starter: '# Список + цикл + f-строка\n',
          expect: 'Обрабатываю: сократи\nОбрабатываю: переведи',
          solution: 'prompts = ["сократи", "переведи"]\nfor p in prompts:\n    print(f"Обрабатываю: {p}")',
          hints: [
            'Внутри тела цикла работает всё, что ты знаешь, — в том числе f-строка с переменной цикла.',
            'prompts = ["сократи", "переведи"]\nfor p in prompts:\n    print(f"Обрабатываю: {___}")',
            'for p in prompts:\n    print(___"Обрабатываю: {p}") — не забудь букву f перед кавычками.',
          ],
          explain: 'F-строка подставляет переменную цикла в шаблон на каждом витке — так печатают отчёты по списку промптов.',
        },
      ],
      quiz: [
        {
          type: 'mcq',
          q: 'Сколько раз выполнится тело цикла for i in range(3)?',
          options: ['2', '3', '4', 'Бесконечно'],
          correct: 1,
          explain: 'range(3) даёт три числа: 0, 1 и 2. Тело цикла выполнится по одному разу для каждого — итого три раза.',
        },
        {
          type: 'tf',
          q: 'range(3) даёт числа 1, 2, 3.',
          correct: false,
          explain: 'range(3) начинает с нуля и не включает верхнюю границу: получаются 0, 1, 2. Та же логика, что у индексов и срезов.',
        },
        {
          type: 'fill',
          q: 'Напечатай каждый промпт из списка:',
          code: 'prompts = ["сократи", "переведи"]\nfor p ___ prompts:\n    print(p)',
          options: ['in', 'of', '='],
          correct: 0,
          explain: 'Синтаксис цикла — for переменная in список. Слово in здесь связывает переменную с элементами списка.',
        },
        {
          type: 'mcq',
          q: 'Что выведет for i in range(2): print(i)?',
          options: ['1 и 2', '0 и 1', '0, 1 и 2', 'Ничего'],
          correct: 1,
          explain: 'range(2) — это числа 0 и 1: старт с нуля, двойка не включается.',
        },
        {
          type: 'order',
          q: 'Собери цикл перебора промптов по порядку:',
          items: [
            'Создать список prompts',
            'Написать строку for prompt in prompts:',
            'Сделать отступ для тела цикла',
            'Внутри цикла вызвать print(prompt)',
          ],
          explain: 'Сначала данные, потом заголовок цикла с двоеточием, а тело — обязательно с отступом, иначе Python не поймёт, что повторять.',
        },
        {
          type: 'code',
          q: 'Циклом for выведи каждый промпт из списка prompts — по одной строке в формате «Промпт: сократи», «Промпт: переведи», «Промпт: объясни».',
          starter: '# Список промптов для обработки\nprompts = ["сократи", "переведи", "объясни"]\n# Выведи каждый в формате: Промпт: <текст>\n',
          expect: 'Промпт: сократи\nПромпт: переведи\nПромпт: объясни',
          solution: 'prompts = ["сократи", "переведи", "объясни"]\nfor p in prompts:\n    print(f"Промпт: {p}")',
          hints: [
            'Заголовок цикла: for p in prompts: — а в теле (с отступом!) один print с f-строкой.',
            'for p in prompts:\n    print(f"Промпт: {___}")',
            'for p in prompts:\n    print(___"Промпт: {p}") — не забудь букву f перед кавычками.',
          ],
          explain: 'Цикл for берёт элементы по одному и кладёт в переменную p, а f-строка подставляет её в шаблон. Три элемента — три строки вывода, и никакого копирования кода.',
        },
      ],
      practice: null,
    },
    {
      id: 'm2-l4',
      title: 'Цикл while и break',
      minutes: 10,
      xp: 30,
      cards: [
        {
          title: 'while — пока условие истинно',
          html: '<p>Цикл <code>for</code> хорош, когда известно, <em>что</em> перебирать. А если нужно повторять, <em>пока</em> что-то верно? Для этого есть <strong>while</strong>: он проверяет условие перед каждым витком и работает, пока оно <code>True</code>.</p>',
          code: 'attempts = 0\nwhile attempts < 3:\n    print("Попытка", attempts)\n    attempts = attempts + 1',
        },
        {
          title: 'Главная опасность',
          html: '<p>Если условие никогда не станет ложным, цикл будет крутиться <strong>бесконечно</strong> и программа зависнет. Забыл строчку <code>attempts = attempts + 1</code> — и всё, вечный цикл.</p>',
        },
        {
          title: 'while True — это не всегда баг',
          html: '<p>Иногда бесконечный цикл пишут <em>специально</em>: <code>while True</code>. Так устроены чат-боты — они крутятся вечно, принимая сообщения, пока пользователь сам не решит уйти.</p>',
          code: 'while True:\n    print("Бот слушает...")',
        },
        {
          title: 'break — аварийный выход',
          html: '<p>Команда <code>break</code> немедленно завершает цикл, где бы она ни стояла. Связка <code>while True</code> + <code>if</code> + <code>break</code> — классический шаблон: работай вечно, но выйди по особому сигналу.</p>',
        },
        {
          title: 'Скелет чат-бота',
          html: '<p>Соединим всё: бот спрашивает сообщение, отвечает и работает, пока не услышит «выход». Это настоящий каркас консольного чат-бота — позже вместо заглушки тут будет ответ реальной LLM.</p>',
          code: 'while True:\n    text = input("Ты: ")\n    if text == "выход":\n        print("Пока!")\n        break\n    print("Бот: я получил твоё сообщение —", text)',
        },
      ],
      drills: [
        {
          afterCard: 0,
          q: 'Замени ___ так, чтобы счётчик рос на единицу и цикл завершился. Ожидаемый вывод: «Попытка 0», «Попытка 1», «Попытка 2».',
          starter: '# Замени ___ — без этой строки цикл вечный!\nattempts = 0\nwhile attempts < 3:\n    print("Попытка", attempts)\n    attempts = ___',
          expect: 'Попытка 0\nПопытка 1\nПопытка 2',
          solution: 'attempts = 0\nwhile attempts < 3:\n    print("Попытка", attempts)\n    attempts = attempts + 1',
          hints: [
            'Новое значение счётчика — это его старое значение плюс один.',
            'attempts = attempts ___ 1 — какой знак стоит между ними?',
            'attempts = ___ + 1 — слева от плюса текущее значение attempts.',
          ],
          explain: 'Строка attempts = attempts + 1 приближает условие к ложному — без неё while крутился бы бесконечно.',
        },
        {
          afterCard: 1,
          q: 'У бота 8 токенов, каждый ответ тратит 3. Напиши цикл: пока tokens > 0, выводи «Токенов осталось: <число>» (получится 8, 5, 2). Убедись, что цикл гарантированно завершается!',
          starter: '# Запас токенов\ntokens = 8\n',
          expect: 'Токенов осталось: 8\nТокенов осталось: 5\nТокенов осталось: 2',
          solution: 'tokens = 8\nwhile tokens > 0:\n    print(f"Токенов осталось: {tokens}")\n    tokens = tokens - 3',
          hints: [
            'Каркас тот же, что с attempts, только счётчик уменьшается: условие tokens > 0, в теле — print и вычитание тройки.',
            'while tokens > 0:\n    print(f"Токенов осталось: {tokens}")\n    tokens = tokens - ___',
            'while tokens ___ 0:\n    print(f"Токенов осталось: {tokens}")\n    tokens = tokens - 3 — в условии знак «больше».',
          ],
          explain: 'Условие проверяется перед каждым витком: 8, 5 и 2 проходят, а -1 — уже нет. Уменьшение счётчика гарантирует завершение цикла.',
        },
        {
          afterCard: 3,
          q: 'С чистого листа: счётчик n стартует с 1. В цикле while True выводи f"Сообщение {n}"; когда n дойдёт до 3 — выйди командой break, иначе увеличивай n. Вывод: Сообщение 1, Сообщение 2, Сообщение 3.',
          starter: '# while True + if + break\n',
          expect: 'Сообщение 1\nСообщение 2\nСообщение 3',
          solution: 'n = 1\nwhile True:\n    print(f"Сообщение {n}")\n    if n == 3:\n        break\n    n = n + 1',
          hints: [
            'Шаблон «работай вечно, выйди по сигналу»: while True, внутри — печать, проверка if и break, затем рост счётчика.',
            'n = 1\nwhile True:\n    print(f"Сообщение {n}")\n    if n == 3:\n        ___\n    n = n + 1',
            'while True:\n    print(f"Сообщение {n}")\n    if n == ___:\n        break\n    n = n + 1 — сравни счётчик с тройкой.',
          ],
          explain: 'while True крутится вечно, но break по условию даёт аварийный выход — классический скелет чат-бота.',
        },
      ],
      quiz: [
        {
          type: 'mcq',
          q: 'Что делает команда break?',
          options: [
            'Пропускает одну итерацию цикла',
            'Немедленно завершает цикл',
            'Перезапускает цикл сначала',
            'Завершает всю программу',
          ],
          correct: 1,
          explain: 'break выходит только из цикла — программа продолжается со строки после него. Всю программу он не останавливает.',
        },
        {
          type: 'tf',
          q: 'Цикл while True без break будет выполняться бесконечно.',
          correct: true,
          explain: 'Условие True никогда не станет ложным, поэтому единственный способ выйти из такого цикла — break внутри.',
        },
        {
          type: 'fill',
          q: 'Заверши чат-бота, когда пользователь пишет «выход»:',
          code: 'while True:\n    text = input("Ты: ")\n    if text == "выход":\n        ___',
          options: ['stop', 'break', 'exit', 'end'],
          correct: 1,
          explain: 'Именно break прерывает цикл. Слов stop и end в Python нет, а exit() завершил бы всю программу целиком.',
        },
        {
          type: 'mcq',
          q: 'Когда while удобнее, чем for?',
          options: [
            'Когда нужно перебрать готовый список',
            'Когда заранее неизвестно, сколько будет повторений',
            'Когда повторений ровно десять',
            'while всегда лучше for',
          ],
          correct: 1,
          explain: 'for — для перебора известного набора, while — «пока что-то верно». Чат-бот не знает, сколько сообщений напишет пользователь, поэтому там while.',
        },
        {
          type: 'order',
          q: 'Расставь шаги работы чат-бота по порядку:',
          items: [
            'Запустить бесконечный цикл while True',
            'Получить сообщение через input()',
            'Проверить, не написал ли пользователь «выход»',
            'Если написал — выполнить break',
          ],
          explain: 'Бот вечно крутится в цикле: читает сообщение, проверяет стоп-слово и выходит по break только по команде пользователя.',
        },
        {
          type: 'code',
          q: 'У бота лимит — 3 запроса к API. Циклом while выведи строки «Запрос 1», «Запрос 2», «Запрос 3», а после цикла — «Лимит исчерпан».',
          starter: '# Счётчик запросов\nn = 1\n# Пока n <= 3: печатай f"Запрос {n}" и увеличивай n на 1\n# После цикла напечатай "Лимит исчерпан"\n',
          expect: 'Запрос 1\nЗапрос 2\nЗапрос 3\nЛимит исчерпан',
          solution: 'n = 1\nwhile n <= 3:\n    print(f"Запрос {n}")\n    n = n + 1\nprint("Лимит исчерпан")',
          hints: [
            'Каркас: while n <= 3 с ростом счётчика внутри (иначе цикл зависнет!), а финальный print — без отступа, после цикла.',
            'while n <= 3:\n    print(f"Запрос {n}")\n    n = ___\nprint("Лимит исчерпан")',
            'while n <= 3:\n    print(f"Запрос {n}")\n    n = n + 1\n___("Лимит исчерпан") — последняя строка БЕЗ отступа.',
          ],
          explain: 'Цикл крутится, пока n <= 3, а строка n = n + 1 гарантирует, что условие однажды станет ложным. Print без отступа не входит в тело цикла, поэтому выполняется один раз — после выхода.',
        },
      ],
      practice: null,
    },
    {
      id: 'm2-l5',
      title: 'Словари (dicts)',
      minutes: 10,
      xp: 30,
      cards: [
        {
          title: 'Данные с подписями',
          html: '<p>В списке элементы лежат по номерам, но у сообщения чата есть <em>смысловые части</em>: кто написал и что написал. Для таких данных есть <strong>словарь</strong> (dict) — хранилище пар <strong>«ключ: значение»</strong>.</p>',
        },
        {
          title: 'Создаём словарь',
          html: '<p>Словарь пишется в <strong>фигурных скобках</strong>: слева ключ, справа значение, между ними двоеточие. Вот сообщение пользователя в чате:</p>',
          code: 'msg = {"role": "user", "content": "Привет, объясни циклы!"}',
        },
        {
          title: 'Доступ по ключу',
          html: '<p>Значение достаётся по ключу в квадратных скобках — как в списке, только вместо номера — имя. Это гораздо понятнее, чем <code>msg[1]</code>.</p>',
          code: 'print(msg["role"])     # user\nprint(msg["content"])  # Привет, объясни циклы!',
        },
        {
          title: 'Добавить и изменить',
          html: '<p>Присвоил значение новому ключу — он добавился. Присвоил существующему — значение заменилось. Никаких специальных методов не нужно.</p>',
          code: 'msg["model"] = "claude"        # добавили ключ\nmsg["content"] = "Новый текст"  # заменили значение',
        },
        {
          title: 'Это формат реального LLM API!',
          html: '<p>Сюрприз: словарь <code>{"role": ..., "content": ...}</code> — это <strong>точный формат сообщений</strong> в API Claude и OpenAI. Роль <code>user</code> — сообщение человека, <code>assistant</code> — ответ модели. Ты только что выучил кусочек настоящего AI-инжиниринга.</p>',
          code: '{"role": "user", "content": "Привет!"}\n{"role": "assistant", "content": "Привет! Чем помочь?"}',
        },
      ],
      drills: [
        {
          afterCard: 1,
          q: 'Замени ___ на строку "user" — роль автора сообщения. Ожидаемый вывод: user',
          starter: '# Замени ___ на значение роли\nmsg = {"role": ___, "content": "Привет, объясни циклы!"}\nprint(msg["role"])',
          expect: 'user',
          solution: 'msg = {"role": "user", "content": "Привет, объясни циклы!"}\nprint(msg["role"])',
          hints: [
            'Значение-строка в словаре пишется в кавычках — как и ключ.',
            'msg = {"role": ___, "content": "Привет, объясни циклы!"} — слово user в кавычках.',
            'msg = {"role": "us___", "content": "Привет, объясни циклы!"} — допиши окончание er.',
          ],
          explain: 'Пара "role": "user" — ключ и значение через двоеточие. Print достал значение по ключу "role".',
        },
        {
          afterCard: 2,
          q: 'Выведи из словаря msg сначала роль (получится user), а на следующей строке — текст сообщения.',
          starter: '# Сообщение чата\nmsg = {"role": "user", "content": "Привет, объясни циклы!"}\n',
          expect: 'user\nПривет, объясни циклы!',
          solution: 'msg = {"role": "user", "content": "Привет, объясни циклы!"}\nprint(msg["role"])\nprint(msg["content"])',
          hints: [
            'Значение достаётся по ключу в квадратных скобках — как индекс, только вместо номера имя.',
            'print(msg[___])\nprint(msg[___]) — подставь два ключа в кавычках.',
            'print(msg["role"])\nprint(msg[___]) — во втором print ключ "content".',
          ],
          explain: 'Квадратные скобки с ключом достают значение: msg["role"] — кто написал, msg["content"] — что написал.',
        },
        {
          afterCard: 3,
          q: "Добавь в словарь msg новый ключ \"model\" со значением \"claude\" и выведи словарь целиком. Ожидаемый вывод: {'role': 'user', 'content': 'Привет!', 'model': 'claude'}",
          starter: '# Сообщение для API\nmsg = {"role": "user", "content": "Привет!"}\n',
          expect: "{'role': 'user', 'content': 'Привет!', 'model': 'claude'}",
          solution: 'msg = {"role": "user", "content": "Привет!"}\nmsg["model"] = "claude"\nprint(msg)',
          hints: [
            'Новый ключ добавляется обычным присваиванием — без специальных методов.',
            'msg[___] = "claude"\nprint(msg) — на месте пропуска ключ в кавычках.',
            'msg["model"] = ___\nprint(msg) — на месте пропуска значение "claude".',
          ],
          explain: 'Присвоил значение новому ключу — он добавился в конец словаря. Print словаря показывает строки в одинарных кавычках.',
        },
        {
          afterCard: 4,
          q: 'С чистого листа: создай словарь-ответ модели с ролью "assistant" и текстом "Чем помочь?". Выведи роль, а на следующей строке — текст.',
          starter: '# Ответ модели в формате LLM API\n',
          expect: 'assistant\nЧем помочь?',
          solution: 'msg = {"role": "assistant", "content": "Чем помочь?"}\nprint(msg["role"])\nprint(msg["content"])',
          hints: [
            'Формат тот же, что у сообщения пользователя, — меняется только значение роли.',
            'msg = {"role": ___, "content": "Чем помочь?"}\nprint(msg["role"])\nprint(msg["content"])',
            'msg = {"role": "assistant", "content": "Чем помочь?"}\nprint(msg[___])\nprint(msg["content"]) — в первом print ключ "role".',
          ],
          explain: 'Роль "assistant" — ответ модели, "user" — реплика человека. Ты собрал сообщение в точном формате API Claude и OpenAI.',
        },
      ],
      quiz: [
        {
          type: 'mcq',
          q: 'Как достать значение по ключу "role" из словаря msg?',
          options: ['msg.role', 'msg("role")', 'msg["role"]', 'msg{role}'],
          correct: 2,
          explain: 'Ключ пишется в квадратных скобках и в кавычках: msg["role"]. Круглые скобки — это вызов функции, тут они не подойдут.',
        },
        {
          type: 'tf',
          q: 'В словаре данные хранятся парами «ключ: значение».',
          correct: true,
          explain: 'В этом суть словаря: у каждого значения есть имя-ключ, по которому его достают. В списке вместо ключей — числовые индексы.',
        },
        {
          type: 'fill',
          q: 'Создай сообщение для LLM API:',
          code: 'msg = {"role": "user", ___: "Привет!"}',
          options: ['content', '"content"', '[content]'],
          correct: 1,
          explain: 'Ключ-строка обязательно пишется в кавычках: "content". Без кавычек Python решит, что это переменная, и выдаст ошибку.',
        },
        {
          type: 'mcq',
          q: 'msg = {"role": "user", "content": "Привет!"} — что выведет print(msg["content"])?',
          options: ['role', 'user', 'Привет!', 'content'],
          correct: 2,
          explain: 'По ключу "content" лежит значение "Привет!". Ключ — это подпись, а print показывает то, что под ней хранится.',
        },
        {
          type: 'tf',
          q: 'В сообщениях LLM API роль "user" означает ответ модели.',
          correct: false,
          explain: 'Наоборот: "user" — это сообщение человека, а ответ модели помечается ролью "assistant".',
        },
        {
          type: 'code',
          q: 'Дан словарь msg — сообщение для LLM API. Выведи одну строку в формате «роль: текст», то есть ровно: user: Объясни словари',
          starter: '# Сообщение в формате LLM API\nmsg = {"role": "user", "content": "Объясни словари"}\n# Выведи: user: Объясни словари\n',
          expect: 'user: Объясни словари',
          solution: 'msg = {"role": "user", "content": "Объясни словари"}\nprint(f"{msg[\'role\']}: {msg[\'content\']}")',
          hints: [
            'Достань оба значения по ключам "role" и "content" и склей одной f-строкой через двоеточие с пробелом.',
            'print(f"{msg[\'role\']}: {___}") — внутри f-строки с двойными кавычками ключи бери в одинарные.',
            'print(f"{msg[___]}: {msg[\'content\']}") — на месте пропуска ключ \'role\' в одинарных кавычках.',
          ],
          explain: 'По ключу "role" лежит "user", по ключу "content" — текст. F-строка склеивает их через двоеточие с пробелом — именно так удобно печатать сообщения чата для отладки.',
        },
      ],
      practice: null,
    },
    {
      id: 'm2-l6',
      title: 'Список словарей = данные API',
      minutes: 10,
      xp: 30,
      cards: [
        {
          title: 'Соединяем список и словарь',
          html: '<p>Одно сообщение — словарь. А вся история чата? <strong>Список словарей</strong>! Именно в таком виде — параметр <code>messages</code> — история диалога отправляется в API Claude и OpenAI. Это не учебный пример, а реальный формат.</p>',
          code: 'messages = [\n    {"role": "user", "content": "Привет!"},\n    {"role": "assistant", "content": "Привет! Чем помочь?"},\n    {"role": "user", "content": "Объясни списки"},\n]',
        },
        {
          title: 'Двойной доступ',
          html: '<p>Сначала индексом достаём словарь из списка, потом ключом — значение из словаря. Читай слева направо: «из messages возьми нулевой элемент, из него — content».</p>',
          code: 'print(messages[0]["content"])   # Привет!\nprint(messages[-1]["role"])     # user',
        },
        {
          title: 'Перебор истории циклом',
          html: '<p>Цикл <code>for</code> выдаёт по одному словарю за виток. Так печатают историю чата — и так же реальный код готовит диалог для модели.</p>',
          code: 'for msg in messages:\n    print(f"{msg[\'role\']}: {msg[\'content\']}")',
        },
        {
          title: 'Новая реплика — append()',
          html: '<p>Пользователь написал новое сообщение? Собираем словарь и добавляем в конец списка. Диалог с ИИ — это буквально <code>append()</code> после каждой реплики.</p>',
          code: 'messages.append({"role": "user", "content": "А что такое словарь?"})',
        },
        {
          title: 'Ты уже читаешь код AI-инженера',
          html: '<p>Открой документацию любого LLM API — увидишь тот самый список словарей. В модуле про API ты передашь такой <code>messages</code> настоящей модели, и она ответит. Вся подготовка уже сделана — этим уроком.</p>',
        },
      ],
      drills: [
        {
          afterCard: 1,
          q: 'Замени ___ на ключ, чтобы вывести ТЕКСТ первого сообщения. Ожидаемый вывод: Привет!',
          starter: '# Замени ___ на нужный ключ\nmessages = [\n    {"role": "user", "content": "Привет!"},\n    {"role": "assistant", "content": "Чем помочь?"},\n]\nprint(messages[0][___])',
          expect: 'Привет!',
          solution: 'messages = [\n    {"role": "user", "content": "Привет!"},\n    {"role": "assistant", "content": "Чем помочь?"},\n]\nprint(messages[0]["content"])',
          hints: [
            'Читай слева направо: messages[0] достал словарь, теперь нужен ключ, под которым лежит текст.',
            'print(messages[0][___]) — ключ-строка пишется в кавычках.',
            'print(messages[0]["cont___"]) — допиши окончание ent.',
          ],
          explain: 'Двойной доступ: сначала индекс достаёт словарь из списка, потом ключ — значение из словаря. Два шага в одной строке.',
        },
        {
          afterCard: 2,
          q: 'Циклом for выведи всю историю в формате «роль: текст» — три строки: user: Привет!, assistant: Рад видеть!, user: Объясни срезы',
          starter: '# История диалога\nmessages = [\n    {"role": "user", "content": "Привет!"},\n    {"role": "assistant", "content": "Рад видеть!"},\n    {"role": "user", "content": "Объясни срезы"},\n]\n',
          expect: 'user: Привет!\nassistant: Рад видеть!\nuser: Объясни срезы',
          solution: 'messages = [\n    {"role": "user", "content": "Привет!"},\n    {"role": "assistant", "content": "Рад видеть!"},\n    {"role": "user", "content": "Объясни срезы"},\n]\nfor m in messages:\n    print(f"{m[\'role\']}: {m[\'content\']}")',
          hints: [
            'for m in messages: выдаёт по словарю за виток; в теле достань m по ключам f-строкой — как в прошлом уроке.',
            'for m in messages:\n    print(f"{m[\'role\']}: {___}") — ключи внутри f-строки в одинарных кавычках.',
            'for m in messages:\n    print(f"{m[___]}: {m[\'content\']}") — на месте пропуска ключ \'role\'.',
          ],
          explain: 'На каждом витке m — очередное сообщение-словарь. Ровно так реальный код печатает диалог перед отправкой в API.',
        },
        {
          afterCard: 3,
          q: 'С чистого листа: создай список messages с одним словарём {"role": "assistant", "content": "Чем помочь?"}. Добавь через append() словарь {"role": "user", "content": "Объясни словари"}. Выведи длину списка (получится 2) и текст последнего сообщения.',
          starter: '# Создай историю, добавь реплику, проверь результат\n',
          expect: '2\nОбъясни словари',
          solution: 'messages = [{"role": "assistant", "content": "Чем помочь?"}]\nmessages.append({"role": "user", "content": "Объясни словари"})\nprint(len(messages))\nprint(messages[-1]["content"])',
          hints: [
            'append() принимает целый словарь как один элемент, а текст последнего — двойной доступ: индекс -1 плюс ключ.',
            'messages = [{"role": "assistant", "content": "Чем помочь?"}]\nmessages.append(___)\nprint(len(messages))\nprint(messages[-1]["content"])',
            'messages.append({"role": "user", "content": "Объясни словари"})\nprint(len(messages))\nprint(messages[___]["content"]) — последний элемент по индексу -1.',
          ],
          explain: 'Диалог с ИИ — это буквально append() словаря после каждой реплики. len() подтвердил, что сообщений стало два.',
        },
      ],
      quiz: [
        {
          type: 'mcq',
          q: 'Что такое messages в API Claude и OpenAI?',
          options: [
            'Одна длинная строка со всей перепиской',
            'Список словарей с ключами role и content',
            'Число сообщений в диалоге',
            'Название нейросети',
          ],
          correct: 1,
          explain: 'История диалога передаётся списком словарей: каждый словарь — одно сообщение с ролью и текстом. Ровно то, что ты собирал в этом уроке.',
        },
        {
          type: 'fill',
          q: 'Достань текст первого сообщения:',
          code: 'messages = [{"role": "user", "content": "Привет"}]\nprint(messages[0][___])',
          options: ['"content"', 'content', '0', '"role"'],
          correct: 0,
          explain: 'Текст лежит под ключом "content" — в кавычках, потому что это строка. По ключу "role" мы получили бы "user", а не текст.',
        },
        {
          type: 'tf',
          q: 'messages[-1] — это последнее сообщение в истории чата.',
          correct: true,
          explain: 'Индекс -1 работает для любого списка, в том числе для списка словарей: он всегда даёт последний элемент — самую свежую реплику.',
        },
        {
          type: 'mcq',
          q: 'Чьё сообщение помечается ролью "assistant"?',
          options: ['Пользователя', 'Модели (ИИ)', 'Разработчика', 'Администратора чата'],
          correct: 1,
          explain: '"assistant" — это ответы модели, "user" — реплики человека. По этим ролям модель понимает, кто что говорил в диалоге.',
        },
        {
          type: 'order',
          q: 'Расставь шаги одного витка диалога с ИИ по порядку:',
          items: [
            'Создать список messages',
            'Добавить словарь с ролью user через append()',
            'Отправить messages модели и получить ответ',
            'Добавить ответ с ролью assistant в конец списка',
          ],
          explain: 'Сначала копим реплику пользователя, отправляем всю историю модели, а её ответ тоже кладём в список — чтобы модель помнила контекст на следующем витке.',
        },
        {
          type: 'code',
          q: 'История чата — список словарей messages. Циклом for выведи каждое сообщение в формате «роль: текст»: первая строка — user: Привет!, вторая — assistant: Чем помочь?',
          starter: '# История диалога в формате LLM API\nmessages = [\n    {"role": "user", "content": "Привет!"},\n    {"role": "assistant", "content": "Чем помочь?"},\n]\n# Выведи каждое сообщение в формате роль: текст\n',
          expect: 'user: Привет!\nassistant: Чем помочь?',
          solution: 'messages = [\n    {"role": "user", "content": "Привет!"},\n    {"role": "assistant", "content": "Чем помочь?"},\n]\nfor m in messages:\n    print(f"{m[\'role\']}: {m[\'content\']}")',
          hints: [
            'Цикл for m in messages: выдаёт по одному словарю за виток; в теле — f-строка с ролью и текстом.',
            'for m in messages:\n    print(f"{m[\'role\']}: {___}") — ключи внутри f-строки в одинарных кавычках.',
            'for m in messages:\n    print(f"{m[___]}: {m[\'content\']}") — на месте пропуска ключ \'role\'.',
          ],
          explain: 'На каждом витке m — очередной словарь-сообщение, из которого мы достаём роль и текст по ключам. Ровно так реальный код печатает историю диалога перед отправкой в API.',
        },
      ],
      practice: null,
    },
    {
      id: 'm2-l7',
      title: 'Функции (def)',
      minutes: 9,
      xp: 30,
      cards: [
        {
          title: 'Код, который можно переиспользовать',
          html: '<p>Один и тот же кусок кода нужен в пяти местах? Копировать — плохая идея: исправлять придётся пять раз. <strong>Функция</strong> (function) — это именованный блок кода, который пишешь один раз, а вызываешь сколько угодно.</p>',
        },
        {
          title: 'Определяем через def',
          html: '<p>Слово <code>def</code>, имя функции, круглые скобки, двоеточие — а тело, как всегда, с отступом.</p>',
          code: 'def say_hello():\n    print("Привет! Я твой AI-ассистент.")',
        },
        {
          title: 'Вызов — имя и скобки',
          html: '<p>Чтобы функция сработала, её нужно <strong>вызвать</strong>: имя плюс круглые скобки. Один def — сколько угодно вызовов.</p>',
          code: 'say_hello()\nsay_hello()\n# Привет! Я твой AI-ассистент.\n# Привет! Я твой AI-ассистент.',
        },
        {
          title: 'Определение — не выполнение',
          html: '<p>Важный момент: строка <code>def</code> только <em>запоминает</em> код. Пока ты не вызовешь функцию, ни одна строчка её тела не выполнится. Определил и не вызвал — на экране пусто.</p>',
        },
        {
          title: 'Ты уже давно пользуешься функциями',
          html: '<p><code>print()</code>, <code>input()</code>, <code>len()</code> — всё это функции, просто <em>встроенные</em>: их написали авторы Python. Теперь ты умеешь создавать свои — из них и собираются программы.</p>',
        },
      ],
      drills: [
        {
          afterCard: 1,
          q: 'Замени ___ на ключевое слово, с которого начинается определение функции. Ожидаемый вывод: Привет! Я твой AI-ассистент.',
          starter: '# Замени ___ на ключевое слово\n___ say_hello():\n    print("Привет! Я твой AI-ассистент.")\n\nsay_hello()',
          expect: 'Привет! Я твой AI-ассистент.',
          solution: 'def say_hello():\n    print("Привет! Я твой AI-ассистент.")\n\nsay_hello()',
          hints: [
            'Это слово — сокращение от английского define, «определить».',
            '___ say_hello(): — три буквы.',
            'de___ say_hello(): — допиши последнюю букву f.',
          ],
          explain: 'def запоминает функцию, а вызов say_hello() выполняет её тело. Один вызов — одна строка вывода.',
        },
        {
          afterCard: 2,
          q: 'Функция уже определена. Вызови её два раза — строка должна появиться дважды.',
          starter: '# Функция готова — вызови её дважды\ndef say_hello():\n    print("Привет! Я твой AI-ассистент.")\n',
          expect: 'Привет! Я твой AI-ассистент.\nПривет! Я твой AI-ассистент.',
          solution: 'def say_hello():\n    print("Привет! Я твой AI-ассистент.")\n\nsay_hello()\nsay_hello()',
          hints: [
            'Вызов — имя функции плюс круглые скобки, на строках без отступа.',
            'say_hello___\nsay_hello___ — чего не хватает после имени?',
            'say_hello()\n___() — на второй строке вызови ту же функцию ещё раз.',
          ],
          explain: 'Один def — сколько угодно вызовов. Каждый вызов выполняет тело функции заново.',
        },
        {
          afterCard: 3,
          q: 'С чистого листа: определи функцию ping, которая печатает «pong». Затем выведи «Старт», вызови ping() и выведи «Стоп» — три строки: Старт, pong, Стоп.',
          starter: '# def только запоминает код — выполняет его вызов\n',
          expect: 'Старт\npong\nСтоп',
          solution: 'def ping():\n    print("pong")\n\nprint("Старт")\nping()\nprint("Стоп")',
          hints: [
            'Сначала определение (оно само ничего не печатает!), потом три строки без отступа — в порядке вывода.',
            'def ping():\n    print("pong")\n\nprint("Старт")\n___\nprint("Стоп")',
            'def ping():\n    print(___)\n\nprint("Старт")\nping()\nprint("Стоп") — в теле печатается строка "pong".',
          ],
          explain: '«pong» появился между «Старт» и «Стоп» — тело функции выполнилось только в момент вызова, а не при определении.',
        },
      ],
      quiz: [
        {
          type: 'mcq',
          q: 'С какого слова начинается определение функции в Python?',
          options: ['func', 'def', 'function', 'make'],
          correct: 1,
          explain: 'def — сокращение от define, «определить». Слов func и function в Python нет.',
        },
        {
          type: 'tf',
          q: 'Код внутри функции выполняется сразу, как только Python прочитал def.',
          correct: false,
          explain: 'def лишь сохраняет функцию в памяти. Тело выполнится только при вызове — имя со скобками.',
        },
        {
          type: 'fill',
          q: 'Определи функцию приветствия:',
          code: '___ say_hello():\n    print("Привет!")\n\nsay_hello()',
          options: ['def', 'func', 'make'],
          correct: 0,
          explain: 'Определение функции всегда начинается со слова def. Дальше — имя, скобки и двоеточие.',
        },
        {
          type: 'mcq',
          q: 'Функцию определили один раз, а вызвали три раза. Сколько раз выполнится её тело?',
          options: ['1', '3', '4', '0'],
          correct: 1,
          explain: 'Тело выполняется при каждом вызове: три вызова — три выполнения. Само определение не считается.',
        },
        {
          type: 'order',
          q: 'Собери создание и использование функции по порядку:',
          items: [
            'Написать def и имя функции с двоеточием',
            'Написать тело функции с отступом',
            'Вызвать функцию по имени со скобками',
          ],
          explain: 'Сначала определение — заголовок и тело с отступом, и только потом вызов. Вызвать функцию до её определения нельзя.',
        },
        {
          type: 'code',
          q: 'Определи функцию bot_hello, которая печатает строку «Бот: готов к работе!», и вызови её два раза — на экране эта строка должна появиться дважды.',
          starter: '# Определи функцию bot_hello через def и вызови её дважды\n',
          expect: 'Бот: готов к работе!\nБот: готов к работе!',
          solution: 'def bot_hello():\n    print("Бот: готов к работе!")\n\nbot_hello()\nbot_hello()',
          hints: [
            'Схема: def bot_hello(): — тело с отступом — затем два вызова без отступа. Без вызова экран останется пустым.',
            'def bot_hello():\n    print("Бот: готов к работе!")\n\n___\n___ — две одинаковые строки-вызова.',
            'def bot_hello():\n    print("Бот: готов к работе!")\n\nbot_hello()\n___() — второй вызов той же функции.',
          ],
          explain: 'def сохранил функцию в памяти, а каждый вызов bot_hello() выполнил её тело заново. Один раз определили — сколько угодно раз используем, в этом и смысл функций.',
        },
      ],
      practice: null,
    },
    {
      id: 'm2-l8',
      title: 'Параметры и return',
      minutes: 11,
      xp: 30,
      cards: [
        {
          title: 'Параметры — вход функции',
          html: '<p>Функция становится по-настоящему полезной, когда принимает данные. <strong>Параметр</strong> — переменная в скобках при определении, <strong>аргумент</strong> — конкретное значение при вызове.</p>',
          code: 'def greet(name):\n    print(f"Привет, {name}!")\n\ngreet("Дима")   # Привет, Дима!\ngreet("Анна")   # Привет, Анна!',
        },
        {
          title: 'return — выход функции',
          html: '<p><code>return</code> <strong>возвращает</strong> результат тому, кто вызвал функцию. Результат можно сохранить в переменную и использовать дальше — вот главное отличие от <code>print</code>, который только показывает текст на экране.</p>',
          code: 'def double(x):\n    return x * 2\n\nresult = double(5)\nprint(result)        # 10\nprint(double(5) + 1) # 11 — с print так не выйдет',
        },
        {
          title: 'return завершает функцию',
          html: '<p>Как только выполнился <code>return</code>, функция закончилась — код после него не выполнится никогда. Этим удобно пользоваться вместе с <code>if</code>: проверил условие — вернул ответ — вышел.</p>',
          code: 'def check_prompt(prompt):\n    if prompt == "":\n        return "Промпт пустой!"\n    return "Промпт готов к отправке"',
        },
        {
          title: 'Значения по умолчанию',
          html: '<p>Параметру можно задать <strong>значение по умолчанию</strong>: если аргумент не передали, возьмётся оно. Передали — значение по умолчанию игнорируется.</p>',
          code: 'def greet(name="друг"):\n    print(f"Привет, {name}!")\n\ngreet()        # Привет, друг!\ngreet("Дима")  # Привет, Дима!',
        },
        {
          title: 'Так выглядят AI-библиотеки',
          html: '<p>Функции реальных AI-библиотек устроены ровно так: обязательный промпт плюс параметры по умолчанию. Скоро ты будешь вызывать такие функции по-настоящему.</p>',
          code: 'def ask(prompt, model="claude", temperature=1.0):\n    return f"[{model}] отвечает на: {prompt}"\n\nprint(ask("Что такое Python?"))',
        },
      ],
      drills: [
        {
          afterCard: 0,
          q: 'Замени ___ на имя параметра, которое использует тело функции. Ожидаемый вывод: Привет, Дима! и Привет, Анна!',
          starter: '# Замени ___ на имя параметра\ndef greet(___):\n    print(f"Привет, {name}!")\n\ngreet("Дима")\ngreet("Анна")',
          expect: 'Привет, Дима!\nПривет, Анна!',
          solution: 'def greet(name):\n    print(f"Привет, {name}!")\n\ngreet("Дима")\ngreet("Анна")',
          hints: [
            'Посмотри, какая переменная подставляется в f-строку внутри тела, — её и нужно объявить параметром.',
            'def greet(___): — то же слово, что в фигурных скобках f-строки.',
            'def greet(na___): — допиши окончание me.',
          ],
          explain: 'Параметр name принимает то, что передали при вызове: сначала "Дима", потом "Анна". Одна функция — разные данные.',
        },
        {
          afterCard: 1,
          q: 'Определи функцию double(x), которая ВОЗВРАЩАЕТ x * 2 через return. Выведи double(5) (получится 10), а на следующей строке — double(5) + 1 (получится 11).',
          starter: '# Функция должна возвращать результат, а не печатать его\n',
          expect: '10\n11',
          solution: 'def double(x):\n    return x * 2\n\nprint(double(5))\nprint(double(5) + 1)',
          hints: [
            'Именно return (не print!) позволяет использовать результат дальше — например, прибавить к нему единицу.',
            'def double(x):\n    ___ x * 2\n\nprint(double(5))\nprint(double(5) + 1)',
            'def double(x):\n    return ___\n\nprint(double(5))\nprint(double(5) + 1) — верни x * 2.',
          ],
          explain: 'return отдаёт значение наружу, поэтому с ним можно считать дальше: double(5) + 1. С print внутри функции так не вышло бы.',
        },
        {
          afterCard: 3,
          q: 'С чистого листа: определи функцию ask(prompt, model="claude"), возвращающую строку «[<model>] <prompt>». Выведи ask("Что такое Python?") и ask("Привет", "gpt") — получится: [claude] Что такое Python? и [gpt] Привет',
          starter: '# Параметр по умолчанию, как в AI-библиотеках\n',
          expect: '[claude] Что такое Python?\n[gpt] Привет',
          solution: 'def ask(prompt, model="claude"):\n    return f"[{model}] {prompt}"\n\nprint(ask("Что такое Python?"))\nprint(ask("Привет", "gpt"))',
          hints: [
            'Значение по умолчанию задаётся прямо в скобках определения: model="claude". Верни f-строку и печатай результаты вызовов.',
            'def ask(prompt, model=___):\n    return f"[{model}] {prompt}"\n\nprint(ask("Что такое Python?"))\nprint(ask("Привет", "gpt"))',
            'def ask(prompt, model="claude"):\n    return f"[{___}] {prompt}" — в квадратных скобках шаблона подставляется model.',
          ],
          explain: 'Первый вызов не передал model — сработало "claude" по умолчанию. Второй передал "gpt" — умолчание проигнорировалось. Так устроены функции реальных AI-библиотек.',
        },
      ],
      quiz: [
        {
          type: 'mcq',
          q: 'Что делает return?',
          options: [
            'Печатает значение на экран',
            'Возвращает значение и завершает функцию',
            'Повторяет функцию ещё раз',
            'Удаляет функцию из памяти',
          ],
          correct: 1,
          explain: 'return отдаёт результат наружу и сразу выходит из функции. Печатью занимается print — это разные вещи.',
        },
        {
          type: 'tf',
          q: 'Код, стоящий внутри функции после выполненного return, не выполнится.',
          correct: true,
          explain: 'return — точка выхода: функция завершается немедленно, и всё, что ниже, Python просто не достигнет.',
        },
        {
          type: 'fill',
          q: 'Верни удвоенное число, чтобы результат можно было сохранить:',
          code: 'def double(x):\n    ___ x * 2\n\nresult = double(5)\nprint(result)  # 10',
          options: ['print', 'return', 'result'],
          correct: 1,
          explain: 'Только return передаст значение в переменную result. С print функция показала бы число на экране, но вернула бы пустоту (None).',
        },
        {
          type: 'mcq',
          q: 'def greet(name="друг"): print(f"Привет, {name}!") — что выведет вызов greet() без аргумента?',
          options: ['Ошибка', 'Привет, name!', 'Привет, друг!', 'Ничего'],
          correct: 2,
          explain: 'Аргумент не передан — сработало значение по умолчанию "друг". Ошибки не будет именно благодаря ему.',
        },
        {
          type: 'tf',
          q: 'Функция может принимать несколько параметров, разделённых запятыми.',
          correct: true,
          explain: 'Параметров может быть сколько угодно: def ask(prompt, model, temperature). При вызове аргументы передаются в том же порядке.',
        },
        {
          type: 'code',
          q: 'Определи функцию make_prompt(topic), которая ВОЗВРАЩАЕТ строку «Объясни простыми словами: <тема>». Выведи её результат для тем "циклы" и "словари" — две строки.',
          starter: '# Функция-шаблон промпта: принимает тему, возвращает готовый промпт\n# Формат: Объясни простыми словами: <тема>\n',
          expect: 'Объясни простыми словами: циклы\nОбъясни простыми словами: словари',
          solution: 'def make_prompt(topic):\n    return f"Объясни простыми словами: {topic}"\n\nprint(make_prompt("циклы"))\nprint(make_prompt("словари"))',
          hints: [
            'Внутри функции — return с f-строкой (именно return, не print), затем два print с вызовами функции.',
            'def make_prompt(topic):\n    return f"Объясни простыми словами: {___}"\n\nprint(make_prompt("циклы"))\nprint(make_prompt("словари"))',
            'def make_prompt(topic):\n    ___ f"Объясни простыми словами: {topic}" — начни строку со слова return.',
          ],
          explain: 'return отдаёт готовую строку наружу, поэтому результат можно передать в print — или куда угодно ещё. Такие функции-шаблоны промптов AI-инженеры пишут постоянно.',
        },
      ],
      practice: null,
    },
    {
      id: 'm2-l9',
      title: 'Мини-проект: анализатор промпта',
      minutes: 12,
      xp: 30,
      cards: [
        {
          title: 'Собираем всё вместе',
          html: '<p>Финальный проект модуля: <strong>анализатор промпта</strong>. Функция принимает текст промпта и выдаёт отчёт — сколько в нём слов и символов, и достаточно ли он подробный. Такие подсчёты — повседневность AI-инженера: длина промпта напрямую влияет на стоимость запроса к модели.</p>',
        },
        {
          title: 'split() — строка в список слов',
          html: '<p>Метод <code>split()</code> разбивает строку по пробелам и возвращает <strong>список слов</strong>. А длину списка ты уже умеешь считать — <code>len()</code>.</p>',
          code: 'text = "объясни циклы простыми словами"\nwords = text.split()\nprint(words)       # ["объясни", "циклы", "простыми", "словами"]\nprint(len(words))  # 4',
        },
        {
          title: 'len() для строки — символы',
          html: '<p>Тот же <code>len()</code>, применённый к строке, считает <strong>символы</strong>, включая пробелы. Слова — через <code>len(text.split())</code>, символы — через <code>len(text)</code>.</p>',
          code: 'print(len("привет"))  # 6',
        },
        {
          title: 'Каркас проекта',
          html: '<p>Вот полный каркас анализатора — тут функция, параметр, <code>split()</code>, <code>len()</code>, f-строки и <code>if/else</code>. Прочитай его строка за строкой: ты понимаешь каждую!</p>',
          code: 'def analyze_prompt(prompt):\n    words = prompt.split()\n    word_count = len(words)\n    char_count = len(prompt)\n    print(f"Слов: {word_count}")\n    print(f"Символов: {char_count}")\n    if word_count < 5:\n        print("Короткий промпт — добавь деталей!")\n    else:\n        print("Хороший подробный промпт.")\n\nanalyze_prompt("Напиши план статьи про Python для новичков")',
        },
        {
          title: 'Идеи для прокачки',
          html: '<p>Работает? Улучши: <ul><li>перебери <code>words</code> циклом <code>for</code> и найди самое длинное слово;</li><li>добавь параметр по умолчанию <code>limit=5</code> вместо «магической» пятёрки;</li><li>прогони через анализатор список из нескольких промптов.</li></ul></p>',
        },
        {
          title: 'Это уже настоящий код',
          html: '<p>Подсчёт слов и символов — упрощённая версия <strong>подсчёта токенов</strong>, за которые берут деньги все LLM API. В модуле про API ты заменишь приблизительный подсчёт на точный — а каркас останется тем же.</p>',
        },
      ],
      drills: [
        {
          afterCard: 1,
          q: "Замени ___ на метод, который разбивает строку на список слов. Ожидаемый вывод: ['объясни', 'циклы', 'простыми', 'словами'] и 4",
          starter: '# Замени ___ на нужный метод\ntext = "объясни циклы простыми словами"\nwords = text.___()\nprint(words)\nprint(len(words))',
          expect: "['объясни', 'циклы', 'простыми', 'словами']\n4",
          solution: 'text = "объясни циклы простыми словами"\nwords = text.split()\nprint(words)\nprint(len(words))',
          hints: [
            'Метод называется split — «разделить»: он режет строку по пробелам и возвращает список.',
            'words = text.___() — имя метода из заголовка карточки, пять букв.',
            'words = text.spl___() — допиши окончание it.',
          ],
          explain: 'split() разрезал строку по пробелам на список из четырёх слов, а len() их посчитал — база любого анализатора промптов.',
        },
        {
          afterCard: 2,
          q: 'Для строки text выведи количество символов (получится 13), а на следующей строке — количество слов (получится 2).',
          starter: '# Промпт\ntext = "объясни циклы"\n',
          expect: '13\n2',
          solution: 'text = "объясни циклы"\nprint(len(text))\nprint(len(text.split()))',
          hints: [
            'Один и тот же len(): для строки он считает символы, для списка слов — слова.',
            'print(len(text))\nprint(len(text.___())) — во второй строке сначала разбей строку на слова.',
            'print(len(___))\nprint(len(text.split())) — в первом print передай саму строку text.',
          ],
          explain: 'len(text) посчитал символы (пробел — тоже символ!), а len(text.split()) — слова. Эта пара — основа отчёта анализатора.',
        },
        {
          afterCard: 3,
          q: 'Определи функцию count_words(prompt), которая ВОЗВРАЩАЕТ число слов через len(prompt.split()). Выведи результат для "Напиши план статьи" (получится 3) и для "объясни" (получится 1).',
          starter: '# Функция подсчёта слов промпта\n',
          expect: '3\n1',
          solution: 'def count_words(prompt):\n    return len(prompt.split())\n\nprint(count_words("Напиши план статьи"))\nprint(count_words("объясни"))',
          hints: [
            'Тело функции — одна строка: верни len от списка слов.',
            'def count_words(prompt):\n    return len(___)\n\nprint(count_words("Напиши план статьи"))\nprint(count_words("объясни"))',
            'def count_words(prompt):\n    ___ len(prompt.split()) — начни строку тела со слова return.',
          ],
          explain: 'split() + len() внутри функции с return — и подсчёт слов применим к любому промпту. Это упрощённый подсчёт токенов, за которые берут деньги LLM API.',
        },
        {
          afterCard: 4,
          q: 'С чистого листа — мини-анализатор: определи функцию analyze(prompt), которая возвращает "коротко", если слов меньше 3, иначе — "отлично". Выведи результат для "объясни циклы" и для "объясни циклы простыми словами" — получится: коротко и отлично.',
          starter: '# Анализатор: меньше 3 слов — "коротко", иначе — "отлично"\n',
          expect: 'коротко\nотлично',
          solution: 'def analyze(prompt):\n    if len(prompt.split()) < 3:\n        return "коротко"\n    return "отлично"\nprint(analyze("объясни циклы"))\nprint(analyze("объясни циклы простыми словами"))',
          hints: [
            'return завершает функцию сразу, поэтому после if можно писать второй return без else.',
            'def analyze(prompt):\n    if len(prompt.split()) < 3:\n        return ___\n    return "отлично"\n\nprint(analyze("объясни циклы"))\nprint(analyze("объясни циклы простыми словами"))',
            'def analyze(prompt):\n    if len(prompt.split()) ___ 3:\n        return "коротко"\n    return "отлично" — в условии знак «меньше».',
          ],
          explain: 'return завершает функцию сразу, поэтому второй return сработает только когда условие ложно. def, if, split, len и return — весь модуль в шести строках.',
        },
      ],
      quiz: [
        {
          type: 'mcq',
          q: 'Что вернёт "как дела у тебя".split()?',
          options: [
            '["как дела у тебя"]',
            '["как", "дела", "у", "тебя"]',
            '4',
            '"какделаутебя"',
          ],
          correct: 1,
          explain: 'split() режет строку по пробелам и возвращает список слов. Число слов из него достаёт уже len().',
        },
        {
          type: 'mcq',
          q: 'Как посчитать количество слов в промпте prompt?',
          options: [
            'len(prompt)',
            'len(prompt.split())',
            'prompt.count()',
            'words(prompt)',
          ],
          correct: 1,
          explain: 'Сначала split() превращает строку в список слов, потом len() считает элементы. len(prompt) посчитал бы символы, а не слова.',
        },
        {
          type: 'tf',
          q: 'len("привет") вернёт 6.',
          correct: true,
          explain: 'Для строки len() считает символы: в слове «привет» их шесть.',
        },
        {
          type: 'fill',
          q: 'Разбей промпт на слова:',
          code: 'def analyze_prompt(prompt):\n    words = prompt.___()',
          options: ['split', 'len', 'words'],
          correct: 0,
          explain: 'split() — метод строки, поэтому вызывается через точку: prompt.split(). len — отдельная функция, а не метод.',
        },
        {
          type: 'order',
          q: 'Расставь шаги работы анализатора промпта по порядку:',
          items: [
            'Определить функцию analyze_prompt(prompt)',
            'Разбить строку на слова через split()',
            'Посчитать слова и символы через len()',
            'Вывести отчёт через print и f-строки',
            'Вызвать функцию с тестовым промптом',
          ],
          explain: 'Классическая структура программы: определяем функцию с логикой «разбить — посчитать — показать», а в самом конце вызываем её с реальными данными.',
        },
        {
          type: 'code',
          q: 'Мини-анализатор: определи функцию analyze(prompt), которая считает слова через len(prompt.split()) и возвращает «Слов: <n> — коротко», если слов меньше 5, иначе «Слов: <n> — отлично». Выведи результат для промпта "Напиши подробный план статьи про Python" (в нём 6 слов).',
          starter: '# Анализатор промпта: слова считаются как len(prompt.split())\n# Верни "Слов: <n> — коротко" (меньше 5) или "Слов: <n> — отлично"\n',
          expect: 'Слов: 6 — отлично',
          solution: 'def analyze(prompt):\n    n = len(prompt.split())\n    if n < 5:\n        return f"Слов: {n} — коротко"\n    return f"Слов: {n} — отлично"\nprint(analyze("Напиши подробный план статьи про Python"))',
          hints: [
            'Внутри функции: посчитай n = len(prompt.split()), верни один из двух вариантов через if и второй return. В конце print(analyze(...)).',
            'def analyze(prompt):\n    n = len(prompt.split())\n    if n < 5:\n        return f"Слов: {n} — коротко"\n    return ___\nprint(analyze("Напиши подробный план статьи про Python"))',
            'def analyze(prompt):\n    n = len(prompt.split())\n    if n ___ 5:\n        return f"Слов: {n} — коротко"\n    return f"Слов: {n} — отлично" — в условии знак «меньше».',
          ],
          explain: 'split() разбил строку на 6 слов, len() их посчитал. Условие 6 < 5 ложно, поэтому первый return не сработал и функция вернула вариант «отлично». Здесь встретилось всё: def, return, if, len, split и f-строки.',
        },
      ],
      practice: {
        html: '<p>Собери анализатор целиком: скопируй каркас из карточки в онлайн-интерпретатор, запусти и прогони через него 2–3 своих реальных промпта. Затем возьми хотя бы одну идею из карточки «Идеи для прокачки» — например, поиск самого длинного слова циклом <code>for</code>.</p>',
        url: 'https://www.online-python.com',
        urlLabel: 'Открыть online-python.com',
      },
    },
  ],
};
